import React from 'react';
import ReactDOM from 'react-dom';


const root = ReactDOM.createRoot(document.getElementById('root'));

const heading = <h1>Hello Woerld form reactElement!</h1>;

root.render(heading);